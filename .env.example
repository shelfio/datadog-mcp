# Datadog MCP Server Configuration
#
# NO CONFIGURATION REQUIRED FOR DEFAULT SETUP!
#
# By default, the server uses AWS Secrets Manager with these settings:
#   - Profile: default
#   - Region: us-west-2
#   - API Key: /DEVELOPMENT/datadog/API_KEY
#   - App Key: /DEVELOPMENT/datadog/APP_KEY
#
# Just run 'aws sso login' and start the server!

# =============================================================================
# AWS Secrets Manager Configuration (all optional - defaults provided)
# =============================================================================

# AWS profile to use (default: "default")
# AWS_PROFILE=default

# AWS region (default: us-west-2)
# AWS_REGION=us-west-2

# Secret paths - override if your secrets are stored elsewhere
# AWS_SECRET_API_KEY=/DEVELOPMENT/datadog/API_KEY
# AWS_SECRET_APP_KEY=/DEVELOPMENT/datadog/APP_KEY

# Optional: Assume a different IAM role
# AWS_ROLE_ARN=arn:aws:iam::123456789012:role/datadog-mcp-role

# Cache TTL in seconds (default: 3000 = 50 minutes)
# SECRET_CACHE_TTL=3000

# =============================================================================
# Alternative: Cookie Authentication
# =============================================================================
# Use browser cookies instead of AWS Secrets Manager.
# Cookie auth takes priority when configured.

# DD_COOKIE=your_cookie_string_here

# Cookie file location (default: ~/.datadog_cookie)
# DD_COOKIE_FILE=/path/to/cookie/file

# CSRF token (required for some endpoints with cookie auth)
# DD_CSRF_TOKEN=your_csrf_token_here
# Or store in file: ~/.datadog_csrf
