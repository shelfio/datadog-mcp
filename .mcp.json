{
  "mcpServers": {
    "datadog": {
      "command": "uv",
      "args": ["run", "datadog-mcp"],
      "env": {
        "DD_API_KEY": "${DD_API_KEY}",
        "DD_APP_KEY": "${DD_APP_KEY}"
      }
    }
  }
}
